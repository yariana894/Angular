import { MayusculasParesPipe } from './mayusculas-pares.pipe';

describe('MayusculasParesPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusculasParesPipe();
    expect(pipe).toBeTruthy();
  });
});
